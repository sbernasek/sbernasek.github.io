source 'https://rubygems.org' '_plugins'

gem "github-pages", '197', group: :jekyll_plugins

# enable tzinfo-data for local build
# gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw]
gem 'jekyll-paginate'

group :jekyll_plugins do
  gem "jekyll-pdf-embed"
  gem "accordion"
end
